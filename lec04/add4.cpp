#include<bits/stdc++.h>

using namespace std;

int main() {
    int a, b;
    int T;
    cin >> T;
    while (T--) {
        cin >> a >> b;
        cout << a + b << endl;
    }

    return 0;
}
