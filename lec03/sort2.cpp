#include<bits/stdc++.h>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    int Min, Max;
    Min = min(a, b);
    Max = max(a, b);
    cout << Min << " " << Max << endl;
    return 0;
}
